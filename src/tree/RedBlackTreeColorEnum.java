package tree;

/**
 * This enumeration creates two well-known, "global"
 * values and gives them a human readable name, representing
 * whether a given node is in a RED or BLACK state.
 */
public class RedBlackTreeColorEnum {
    public static final int RED = 0;
    public static final int BLACK = 1;
}
